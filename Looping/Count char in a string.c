#include<stdio.h>
int main()
{
    int count;
    char c,d,e;
    String str;
    int res = 0;
    for (int i=0; i<s.length(); i++)
        {
            // checking character in string
            if (s.charAt(i) == c)
            res++;
        }
        return res;
    }
    {
        String str= "Programming is good habit";
        char c = 'm';
        char d = '0';
        char e = 'r';
        System.out.println(count(str, c,d,e));
    }
}
